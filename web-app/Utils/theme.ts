import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    // Emerald
    primary: {
      main: '#3CC47C',
    },
    // Venetian Red
    secondary: {
      main: '#C90E0E',
    },
  },
  //tonalOffset: 0.1,
});

export default theme;
